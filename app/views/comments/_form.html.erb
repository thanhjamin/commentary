<%= form_for @comment do |f| %>
  <% if @comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% @comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.hidden_field :parent_id %>
  <p class="field">
    <%= f.label :full_name %><br>
    <%= f.text_field :full_name, placeholder: "Jon Snow", required: true %>
  </p>
  <p class="field">
    <%= f.label :email %><br>
    <%= f.email_field :email, placeholder: "JonSnow@CastleBlack.com", required: true %>
  </p>
  <p>
    <%= f.label :comment, "New Comment" %><br />
    <%= f.text_area :comment, rows: 8, placeholder: "I know nothing", required: true %>
  </p>
  <p><%= f.submit "Save", class: "btn" %></p>
<% end %>

<% if session[:comment_ids] && session[:comment_ids].include?(comment.id) %>
<p>hello world</p>
<% end %> 

